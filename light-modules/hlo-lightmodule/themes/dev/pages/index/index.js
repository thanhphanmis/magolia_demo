// 1. We import the template scripts

// We put the page style import at the end of import sequence, to make sure this can override any component style in case
import './style.scss'

$(function() {

});